query SearchJobs($input: SearchJobsInput!) {
  searchJobs(input: $input) {
    id
    title
    description
    location
    salary
    type
    remote
    isApplied
    createdAt
    company {
      id
      name
    }
  }
}
