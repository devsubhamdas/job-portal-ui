fragment User on User {
  id
  name
  email
  role
}

mutation Login($input: LoginInput!) {
  login(input: $input) {
    ...User
  }
}

mutation Logout {
  logout
}

query Me {
  me {
    ...User
  }
}

mutation Signup($input: SignupInput!) {
  signup(input: $input) {
    ...User
  }
}
